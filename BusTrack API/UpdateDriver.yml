info:
  name: UpdateDriver
  type: http
  seq: 6

http:
  method: PUT
  url: http://localhost:3000/drivers/2
  body:
    type: json
    data: |-
      {
        "nombre":"Julia roberts",
        "license":"yanovoy"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
