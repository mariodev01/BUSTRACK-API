info:
  name: Crear
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/drivers/drivers
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
